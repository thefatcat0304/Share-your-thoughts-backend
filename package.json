{
  "name": "json_server",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "json-server db.json --host 0.0.0.0 --port $PORT",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}